class Player {
  constructor(){}

  getCount(){
   
    playerCountRef.on("value",function(data){
      
    })
  }

  updateCount(count){
    database.ref('/').update({
      
    });
  }

  update(name){
    
    database.ref(playerIndex).set({
      
    });
  }
  
  
}
